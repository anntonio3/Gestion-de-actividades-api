package mx.edu.unpa.actividadesapi.enums;

/**
 * Opciones de sexo para el registro de inscripcion de externos (US-24).
 * Se persiste como STRING en la columna ENUM de la BD.
 */
public enum Sexo {
    MASCULINO,
    FEMENINO,
    OTRO
}
